import styled from "styled-components";

const H2 = styled.h2`
  font-family: "Bellefair", sans-serif;
  color: var(--white);
  font-size: 100px;
  padding: 30px 0;
  @media (width <= 600px) {
    font-size: 56px;
  }
`;

export default H2;

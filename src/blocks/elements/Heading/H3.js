import styled from "styled-components";

const H3 = styled.h3`
  font-family: "Bellefair", sans-serif;
  font-size: 56px;
  color: var(--white);
  @media (width <= 600px) {
    font-size: 24px;
  }
`;

export default H3;
